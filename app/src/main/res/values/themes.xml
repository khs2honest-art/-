<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      We keep an app theme resource so AndroidManifest can reference it,
      but use a built-in Android theme to avoid needing Material (Views) themes.
    -->
    <style name="Theme.TapCirclesApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here if needed. -->
    </style>
</resources>
