# TapCirclesApp

Android app (Jetpack Compose) with three circle buttons labeled 1/2/3.
Tap a circle to save the current date/time and show it to the right.

Build is set up for GitHub Actions (see `.github/workflows/build-apk.yml`).
